name: Build
on:
  push:
    branches:
      - main
      - dev
  pull_request:
    types: [opened, synchronize, reopened]
jobs:
  buildEngine:
    name: Build Engine
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: engine
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0  # Shallow clones should be disabled for a better relevancy of analysis
      - name: Set up JDK 11
        uses: actions/setup-java@v1
        with:
          java-version: 11  
      - name: Build Legoliveries Backend
        run: docker-compose build -f ../docker-compose.yml legoliveries
      - name: Start Legoliveries Backend
        run: docker-compose run -d -p 8080:8080 -f ../docker-compose.yml  legoliveries
      - name: Build Legoliveries Frontend
        run: docker-compose build -f ../docker-compose.yml legoliveries-app
      - name: Start Legoliveries Frontend
        run: docker-compose run -d -p 3000:3000 -f ../docker-compose.yml legoliveries-app
      - name: Cache SonarCloud packages
        uses: actions/cache@v1
        with:
          path: ~/.sonar/cache
          key: ${{ runner.os }}-sonar
          restore-keys: ${{ runner.os }}-sonar
      - name: Cache Maven packages
        uses: actions/cache@v1
        with:
          path: ~/.m2
          key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-m2
      - name: Build and analyze
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}  # Needed to get PR information, if any
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN_ENGINE }}
        run: mvn -B verify org.sonarsource.scanner.maven:sonar-maven-plugin:sonar -Dsonar.projectKey=flipgoncalves_TQSProject_engine
  buildRestAPI:
    name: Build restAPI
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: restapi
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0  # Shallow clones should be disabled for a better relevancy of analysis
      - name: Set up JDK 11
        uses: actions/setup-java@v1
        with:
          java-version: 11
      - name: Build Engine Backend
        run: docker-compose build -f ../docker-compose.yml engine
      - name: Start Legoliveries Backend
        run: docker-compose run -p 9001:9001 -d -f ../docker-compose.yml engine
      - name: Build Legoliveries Frontend
        run: docker-compose build -f ../docker-compose.yml engine-app
      - name: Start Legoliveries Frontend
        run: docker-compose run -d -p 3001:3001 -f ../docker-compose.yml engine-app
      - name: Cache SonarCloud packages
        uses: actions/cache@v1
        with:
          path: ~/.sonar/cache
          key: ${{ runner.os }}-sonar
          restore-keys: ${{ runner.os }}-sonar
      - name: Cache Maven packages
        uses: actions/cache@v1
        with:
          path: ~/.m2
          key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-m2
      - name: Build and analyze
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}  # Needed to get PR information, if any
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN_LEGOLIVERIES }}
        run: mvn -B verify org.sonarsource.scanner.maven:sonar-maven-plugin:sonar -Dsonar.projectKey=flipgoncalves_TQSProject_legoliveries